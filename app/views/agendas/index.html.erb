<!DOCTYPE html>
<body>
<head>
</head>
<body></body>
<div class="container">
<table class="table table-condensed table-bordered">
  <thead>
  <tr>
    <th>Nombres</th>
    <th>Apellidos</th>
    <th>Rut</th>
    <th colspan="3"></th>
  </tr>
  </thead>
  <tbody>
  <% @agendas.each do |agenda| %>
  <tr>
  <td><%= agenda.comentario %></td>
  <td><%= agenda.fecha %></td>
  <td><%= agenda.estudiante.nombres %></td>
  <td><%=link_to 'mostrar', agenda_url(agenda) %></td>
  <td><%= link_to 'editar', editar_agenda_url(agenda) %></td>
  <td><%= link_to 'ELIMINAR', agenda_path(agenda),
              method: :delete,
              data: {confirm: '¿Estas seguro?'} %></td>
  </tr>
  </tbody>
</table>
  <a href="/agendas/nuevo" class="btn btn-success btn-lg btn-block" role="button" aria-pressed="true">Nuevo</a>
</div>
</body>
</html>

<%end %>