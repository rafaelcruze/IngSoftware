<% @agendas.each do |agenda| %>
  Agenda <%= agenda.comentario %>
  Fecha <%= agenda.fecha %>
  Estudiante <%= agenda.estudiante.nombres %>
  
  <%=link_to 'mostrar', agenda_url(agenda) %>
  <%= link_to 'editar', editar_agenda_url(agenda) %>
  <%= link_to 'ELIMINAR', agenda_path(agenda),
              method: :delete,
              data: {confirm: '¿Estas seguro?'} %>
  <br>
<%end %>